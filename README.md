# ui-lab

UI components I'm building to learn interaction design.

```bash
bun install
bun dev
```

Add a component with `bun run new <slug>`. Built on [design-scaffold](https://github.com/xevrion/design-scaffold).
